# zzDiary 智能日记本 — 软件设计文档

## 1. 产品概述

**zzDiary** 是一款以正念（Mindfulness）为核心的智能日记分析工具。它通过对话式书写体验，引导用户逐步表达内心难以启齿的想法与情绪，并结合原生家庭回溯，帮助用户理解情绪根源，推进自我觉察。

## 2. 核心功能

### 2.1 引导式日记书写

- AI 以温和、非评判的语气逐轮引导，降低表达门槛。
- 针对用户回避的话题，采用渐进式提问（"不想写也没关系，可以简单说说今天的感受"）。
- 支持自由书写模式与引导模式一键切换。

### 2.2 情绪分析与洞察

- 每篇日记自动生成情绪标签（如焦虑、愤怒、羞耻、喜悦）。
- 输出情绪趋势图（周/月/年），标记情绪拐点。
- 关键句高亮，标注可能的认知偏差（如灾难化、读心术）。

### 2.3 原生家庭回溯

- 在用户允许下，AI 会询问家庭背景（父母关系、童年经历、教养方式）。
- 将当前情绪/行为模式与原生家庭经验关联分析，生成"情绪来源推测"。
- 所有家庭信息仅在本地存储，不上传云端。

### 2.4 正念练习引导

- 书写后推荐匹配当前情绪的正念练习（呼吸练习、身体扫描、慈心冥想）。
- 每日"感恩三件事"轻量模板，培养积极关注习惯。

## 3. 系统架构

```
┌──────────────────────────────────────┐
│              前端 (Desktop)           │
│  ┌──────────┐  ┌──────────────────┐  │
│  │ 日记编辑器 │  │  情绪仪表盘       │  │
│  └──────────┘  └──────────────────┘  │
│  ┌──────────┐  ┌──────────────────┐  │
│  │ 对话引导区 │  │  正念练习播放器    │  │
│  └──────────┘  └──────────────────┘  │
├──────────────────────────────────────┤
│            本地服务层 (Local)          │
│  ┌──────────┐  ┌──────────────────┐  │
│  │ SQLite   │  │  本地向量索引      │  │
│  │ (日记存储) │  │  (语义检索)       │  │
│  └──────────┘  └──────────────────┘  │
├──────────────────────────────────────┤
│          AI 分析引擎 (可选云端)         │
│  ┌────────────────────────────────┐  │
│  │ DeepSeek / Qwen 等开源模型       │  │
│  │ (通过 API 或本地 Ollama 部署)    │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

## 4. 技术选型

| 层次 | 技术 | 说明 |
|------|------|------|
| 桌面壳 | Electron 33.x | 生态最成熟，Node.js 与 Java 进程通信便捷 |
| 前端框架 | React 18 + TypeScript 5.x | 生态最成熟，类型安全 |
| 构建工具 | Vite 6.x | HMR < 100ms，Electron 热重载友好 |
| CSS 方案 | Tailwind CSS 4.x | 原子化 CSS，零运行时开销 |
| 状态管理 | Zustand 5.x | 1KB 体积，无 boilerplate，按需订阅 |
| 路由 | React Router 7.x | 标准 SPA 路由，数据加载模式 |
| 图表 | Recharts 2.x | React 原生 SVG 组件，包体 ~300KB |
| 后端语言 | Java 21 LTS | 虚拟线程、记录类型、模式匹配 |
| 后端框架 | Spring Boot 3.4.x | 生态最全，内嵌 Tomcat，自动配置 |
| 构建工具 | Gradle Kotlin DSL 8.x | 现代化构建，比 Maven 简洁 |
| 本地数据库 | SQLite (JDBC) | 嵌入式零配置，数据不外泄 |
| 数据库加密 | AES-256-GCM (JCA) | 应用层字段级加密，JCA 内建零依赖 |
| 向量检索 | JVector (嵌入式) | 纯 Java，基于 DiskANN 算法，零配置 |
| AI 主模型 | DeepSeek Chat API | 中文 SOTA，成本极低，长上下文 |
| AI 备选 | Ollama 本地部署 | 完全离线，qwen2.5 或 deepseek-r1 |

## 5. 核心数据结构

```
User
  ├── nickname, avatar
  ├── family_background (加密)
  │     ├── childhood_summary
  │     ├── parental_relationship
  │     └── significant_events[]
  └── settings (引导强度、模型偏好)

DiaryEntry
  ├── content (加密文本)
  ├── mode: "guided" | "free"
  ├── emotion_tags[]
  ├── emotion_intensity (1-10)
  ├── cognitive_bias[]
  ├── family_insight_id (关联的家庭分析)
  └── created_at

EmotionInsight
  ├── entry_id
  ├── emotion_type
  ├── possible_root_cause
  ├── family_connection (是否关联原生家庭)
  └── mindfulness_suggestion
```

## 6. AI 引导策略

### 6.1 对话框架

1. **开场** — "今天有什么想聊的吗？"
2. **深化** — 若用户情绪词密度高，追问："这件事让你联想到过去的类似经历吗？"
3. **回溯** — 若已录入家庭背景："你刚才说的这种被忽视的感觉，和你之前提到小时候和妈妈的关系有联系吗？"
4. **收束** — 生成当日小结 + 正念建议。

### 6.2 Prompt 设计原则

- 始终以用户主体性为优先，避免强行分析。
- 使用"可能"、"或许"等开放性措辞，避免诊断式语气。
- 对敏感话题自动切换更温和的表达。

## 7. 隐私与安全

- 所有日记与家庭数据仅存储在本地，默认不上传任何服务器。
- 若使用云端 API（如 DeepSeek），发送前做脱敏处理，且明确告知用户。
- 支持 Ollama 本地模型部署，实现完全离线使用。
- 应用启动可设密码/生物识别锁。

## 8. 开发路线图

| 阶段 | 内容 | 时间 |
|------|------|------|
| MVP | 自由书写 + DeepSeek 情绪分析 + 本地存储 | 4 周 |
| V1.0 | 引导式对话 + 情绪趋势图 + 正念建议 | 6 周 |
| V1.5 | 原生家庭模块 + 加密 + Ollama 支持 | 4 周 |
| V2.0 | 多语言 + 导出报告 + 主题定制 | 持续 |

## 9. 模型调用方案

首选 **DeepSeek Chat API**（`deepseek-chat` 模型），原因：
- 中文理解能力强，适合日记分析场景。
- 免费额度充足，API 价格极低。
- 支持长上下文，便于引入历史日记与家庭背景。

备选方案：通过 **Ollama** 本地运行 `qwen2.5` 或 `deepseek-r1` 蒸馏版，实现离线分析。
